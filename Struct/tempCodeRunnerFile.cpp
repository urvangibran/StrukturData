   // int res;
                // res = ipSearch(dataMahasiswa, cntMahasiswa, NIKSearch);
                // if(res == -1) printf("Data Tidak Ditemukan");
                // else{
                //     displaySearch(res);
                // }
